const expect = require('chai').expect;
const chase = require('../server')

let jesper = 3;

module.exports = 
    describe('chase', function() {
    it('should give right answers for equal and unequal inputs', function(){
        expect(jesper).to.be.a.name;
        
        
        
    })
  }
);